package zone.althaea.freeze.neoforge;

import zone.althaea.freeze.Freeze;
import net.neoforged.fml.common.Mod;

@Mod(Freeze.MOD_ID)
public final class FreezeNeoForge {
    public FreezeNeoForge() {
        // Run our common setup.
        Freeze.init();
    }
}
